# Active scenes live here.
#
# Keep placeholder scenes out of active config so Home Assistant can validate
# cleanly before real entity IDs are known. See docs/ikea_remote_scene_setup.md
# for a current scene example using color_temp_kelvin.
[]
